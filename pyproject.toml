[project]
name = "model-uncertainty-exploration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13, <3.14"
dependencies = [
    "distrax>=0.1.8",
    "flashbax>=0.1.3",
    "flax>=0.10.0",
    "gpjax",
    "gymnasium[classic-control]>=1.2.3",
    "gymnax",
    "jax[cuda12]>=0.9.2",
    "h5py>=3.0",
    "minari>=0.5.3",
    "optax>=0.2.0",
    "pyyaml>=6.0",
    "seaborn>=0.13.2",
    "setuptools<82",
    "tensorboard>=2.20.0",
    "tensorboardx>=2.6.5",
    "tqdm>=4.67.3",
    "tyro>=1.0.12",
    "orbax-checkpoint>=0.5.0",
    "brax>=0.14.2",
]

[tool.uv.sources]
gpjax = { git = "https://github.com/thomaspinder/GPJax.git" }
gymnax = { git = "https://github.com/steventango/gymnax" }

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.15.9",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
